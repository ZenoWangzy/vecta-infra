- name: Write deploy audit (success)
  shell: scripts/write-deploy-audit.sh success "{{ deploy_sha }}" vtest
  args:
    chdir: "{{ playbook_dir }}/.."
    executable: /bin/bash
  environment:
    DATABASE_URL: "{{ lookup('env', 'DATABASE_URL') }}"
  changed_when: true
