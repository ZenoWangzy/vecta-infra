# Production inventory is protected and supplied outside this repository.
# This empty fixture is only for syntax-check/list-tasks; the release workflow
# requires MYPC_INVENTORY_FILE and rejects placeholders before any playbook.
[mypc]

[mypc:vars]
remote_dir=/data/ocee
base_url=https://vecta.matrix-ai.com.cn
mypc_deploy_enabled=false
